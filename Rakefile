# frozen_string_literal: true

# Rakefile generated for personal-site by Quartet
# Add tasks as required here

task :c do
  sh 'bundle exec irb -I. -r app.rb'
end

task :console do
  sh 'bundle exec irb -I. -r app.rb'
end

task :serve do
  sh 'bundle exec rackup config.ru'
end

task :s do
  sh 'bundle exec rackup config.ru'
end
